services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./app:/app/app
      - ./components:/app/components
      - ./lib:/app/lib
      - ./public:/app/public
      - ./next.config.mjs:/app/next.config.mjs
      - ./tsconfig.json:/app/tsconfig.json
      - ./postcss.config.mjs:/app/postcss.config.mjs
      - /app/node_modules
      - /app/.next
    command: npm run dev
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Uncomment below if you have a backend API service
  # backend:
  #   build:
  #     context: ../backend
  #     dockerfile: Dockerfile
  #   container_name: backend
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - NODE_ENV=development
  #   depends_on:
  #     - db
  #   restart: unless-stopped

  # db:
  #   image: postgres:16-alpine
  #   container_name: db
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     - POSTGRES_USER=admin
  #     - POSTGRES_PASSWORD=password
  #     - POSTGRES_DB=analytics
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   db_data:
